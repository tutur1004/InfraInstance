package fr.milekat.hostapi.workers;

import com.google.common.io.ByteArrayDataOutput;
import com.google.common.io.ByteStreams;
import fr.milekat.hostapi.Main;
import org.bukkit.Bukkit;
import org.bukkit.entity.Player;

public class SendManager {/*
    public static void notifyHostJoined(String serverIdentifier, String serverName) {
        ByteArrayDataOutput out = ByteStreams.newDataOutput();
        out.writeUTF(Main.MESSAGE_SUB_CHANNEL);
        out.writeUTF();


        Player player = Bukkit.getPlayerExact("Example");
        player.sendPluginMessage(Main.HOST_PLUGIN, Main.MESSAGE_CHANNEL, out.toByteArray());
    }*/
}
